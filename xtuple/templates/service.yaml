apiVersion: v1
kind: Service
metadata:
  name: {{ include "xtuple.fullname" . }}
  labels:
    {{- include "xtuple.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xtuple.selectorLabels" . | nindent 4 }}
